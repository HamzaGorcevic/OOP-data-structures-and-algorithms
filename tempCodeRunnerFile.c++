t j = 1;
        // while (j != count - 1 && arr[j] < arr[j + 1])
        // {
        //     j++;
        // }
        // temp = arr[j];
        // arr[j] = arr[j + 1];
        // arr[j + 1] = temp;
